# kernl — Claude Instructions

> **STUB.** This file was created by `build-canon.mjs` to record the canonical build
> target. It does NOT contain full project instructions yet. Anyone working this
> project should fill in: production URL, repo, deploy flow, architecture notes,
> and a proper Pre-Flight section.

<!-- BUILD_CANON:START -->
## CANONICAL BUILD

*Managed block. Generated by `D:\Meta\build-canon.mjs` — edit the script, not this text.*

**Canonical build:** LOCAL. This is a MCP server.

**There is no platform build to fall back on.** A failing local build is a REAL
failure here, not noise. Local dependencies must be present and working before
this project can be built or shipped.

Build via D:\Meta\kernl_build.bat from cmd only.

**Confidence:** VERIFIED. Human-confirmed target.

**Package manager:** npm (package-lock.json). Check the lockfile before any install — the wrong
package manager breaks the deploy.

Portfolio rule: `D:\Meta\OPERATING_CONTEXT.md` § BUILD ENVIRONMENTS.
Machine roles: G7 is a workstation and is **nothing canonical** — that applies to
build artifacts and dependencies, not just data.
<!-- BUILD_CANON:END -->
